apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: urlshortener
  labels:
    app: postgres
spec:
  type: ClusterIP
  selector:
    app: postgres
  ports:
    - name: postgres
      port: 5432        # Service port
      targetPort: 5432  # Container port
